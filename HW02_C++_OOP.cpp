#include <iostream>
#include <time.h>

const int MAX_SIZE = 128;
const int TYPE_SIZE = 4;

int main()
{
	srand(time(NULL));
	using std::cout;
	using std::cin;
	using std::endl;

	int arrCode[MAX_SIZE]{};
	int sizeCode = 12;

	cout << "Start!\n";

	return 0;
}